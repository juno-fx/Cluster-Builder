isolation:
  enabled: true

  resourceQuota:
    enabled: false

syncer:
  extraArgs:
    - --tls-san=NAME.NAME.svc.cluster.local

sync:
  storage:
    persistence: false
  ingresses:
    enabled: true
  persistentvolumes:
    enabled: true
  nodes:
    enabled: true
    syncAllNodes: true
