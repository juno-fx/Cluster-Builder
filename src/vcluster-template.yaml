isolation:
  enabled: true

  resourceQuota:
    enabled: false

syncer:
  extraArgs:
    - --tls-san=NAME.NAME.svc.cluster.local

sync:
  storageclasses:
    enabled: true
  storage:
    persistence: false
  persistentvolumes:
    enabled: true
  persistentvolumeclaims:
    enabled: true
  ingressclasses:
    enabled: true
  ingresses:
    enabled: true
  nodes:
    enabled: true
    syncAllNodes: true
